# 生产部署：复制为 .env.prod 后填写密钥
# docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build
# docker compose -f docker-compose.prod.yml --env-file .env.prod exec ptoe_api npm run db:push

PTOE_POSTGRES_PASSWORD=
SESSION_SECRET=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

SESSION_TTL_DAYS=90
ADMIN_SESSION_TTL_HOURS=24
MAX_DEVICES_DEFAULT=2
IDLE_RELEASE_DAYS=30
LOGIN_RATE_LIMIT=5

CONTACT_WECHAT=请联系客服微信（待配置）
CONTACT_EMAIL=support@example.com
COOKIE_SECURE=true
